---
title: Schema Reference
description: Complete field-by-field reference for the ChSON v2 schema
---

# Schema Reference

This page provides a complete reference for all fields in the ChSON v2 schema. For conceptual explanations, see [Core Concepts](/docs/core-concepts) and [Advanced Features](/docs/advanced-features).

## Schema URL

All ChSON v2 files should include the schema reference:

```json
{
  "$schema": "https://chson.dev/schema/v2/chson.schema.json"
}
```

---

## Root Object

The root object contains metadata about the cheatsheet and an array of sections.

### Required Fields

| Field             | Type     | Description                                                                                                  |
| ----------------- | -------- | ------------------------------------------------------------------------------------------------------------ |
| `title`           | `string` | The name of this cheatsheet. Must be non-empty.                                                              |
| `publicationDate` | `string` | When this cheatsheet was published or last updated. Must be ISO 8601 format (`YYYY-MM-DD` or full datetime). |
| `description`     | `string` | A brief summary of what this cheatsheet covers. Must be non-empty.                                           |
| `sections`        | `array`  | Logical groupings of related entries. Must contain at least one section.                                     |

### Optional Fields

| Field                | Type       | Description                                                                        |
| -------------------- | ---------- | ---------------------------------------------------------------------------------- |
| `$schema`            | `string`   | JSON Schema reference URL for validation.                                          |
| `version`            | `string`   | Version of the software this document covers (e.g., `"3.2.1"`).                    |
| `documentType`       | `string`   | Type of document: `cheatsheet`, `checklist`, `runbook`, `tldr`, or `bookmarks`.    |
| `author`             | `string`   | The author or maintainer of this document.                                         |
| `license`            | `string`   | License identifier (e.g., `"MIT"`, `"CC-BY-4.0"`).                                 |
| `homepage`           | `string`   | URL of the official website or documentation for the subject matter.               |
| `icon`               | `string`   | URL or relative path to an image representing this document.                       |
| `tags`               | `string[]` | Keywords for categorization and discovery.                                         |
| `retrievalDirection` | `string`   | Optional rendering hint. See [Retrieval Direction](#retrievaldirection).           |
| `anchorLabel`        | `string`   | Display name for the anchor column. Default: `"Anchor"`.                           |
| `contentLabel`       | `string`   | Display name for the content column. Default: `"Content"`.                         |
| `metadata`           | `object`   | Additional freeform metadata.                                                      |

---

## Field Details

### `title`

```json
{
  "title": "Git Core Commands"
}
```

The human-readable name of the cheatsheet. This is displayed as the main heading and used for identification.

**Constraints:**

- Type: `string`
- Minimum length: 1 character
- Required: Yes

---

### `version`

```json
{
  "version": "2.47.0"
}
```

The version of the software being documented. This helps users know if the cheatsheet applies to their installed version.

**Constraints:**

- Type: `string`
- Minimum length: 1 character
- Required: No

---

### `publicationDate`

```json
{
  "publicationDate": "2026-02-15"
}
```

When this cheatsheet was published or last updated. Accepts either date or datetime format.

**Constraints:**

- Type: `string`
- Format: ISO 8601 date (`YYYY-MM-DD`) or datetime (`YYYY-MM-DDTHH:MM:SSZ`)
- Required: Yes

**Examples:**

- `"2026-02-15"` - Date only
- `"2026-02-15T10:30:00Z"` - Full datetime

---

### `description`

```json
{
  "description": "Essential Git commands for daily development workflows"
}
```

A brief summary explaining what this cheatsheet covers and who it's for.

**Constraints:**

- Type: `string`
- Minimum length: 1 character
- Required: Yes

---

### `retrievalDirection`

```json
{
  "retrievalDirection": "mechanism-to-meaning"
}
```

Optional hint for renderers about the cognitive lookup direction. This may influence how the data is presented but has no effect on validation.

**Constraints:**

- Type: `string`
- Allowed values:
  - `"intent-to-mechanism"` - User knows what they want, needs the command/shortcut
  - `"mechanism-to-meaning"` - User sees a command, needs to understand what it does
- Required: No

See [Advanced Features](/docs/advanced-features#retrieval-direction) for detailed explanation.

---

### `author`

```json
{
  "author": "Jane Developer"
}
```

The author or maintainer of this document.

**Constraints:**

- Type: `string`
- Required: No

---

### `license`

```json
{
  "license": "MIT"
}
```

License identifier for this document. Use standard identifiers like SPDX license IDs.

**Constraints:**

- Type: `string`
- Required: No

**Common values:** `"MIT"`, `"CC-BY-4.0"`, `"CC0-1.0"`, `"Apache-2.0"`

---

### `homepage`

```json
{
  "homepage": "https://git-scm.com"
}
```

URL of the official website or documentation for the subject matter.

**Constraints:**

- Type: `string`
- Format: URI
- Required: No

---

### `icon`

```json
{
  "icon": "https://git-scm.com/images/logos/downloads/Git-Icon-1788C.png"
}
```

URL or relative path to an image representing this document (logo, icon, etc.).

**Constraints:**

- Type: `string`
- Required: No

**Examples:**

- Absolute URL: `"https://example.com/logo.png"`
- Relative path: `"./icons/git.svg"`

---

### `tags`

```json
{
  "tags": ["cli", "devtools", "version-control"]
}
```

Keywords for categorization and discovery.

**Constraints:**

- Type: `array` of `string`
- Required: No

---

### `anchorLabel`

```json
{
  "anchorLabel": "Shortcut"
}
```

Custom display name for the anchor column. Use domain-appropriate terminology.

**Constraints:**

- Type: `string`
- Required: No
- Default: `"Anchor"`

**Common values:**

- `"Command"` - CLI tools
- `"Shortcut"` - Keyboard shortcuts
- `"Example"` - Code samples
- `"Syntax"` - Language references
- `"Option"` - Configuration

---

### `contentLabel`

```json
{
  "contentLabel": "Action"
}
```

Custom display name for the content column.

**Constraints:**

- Type: `string`
- Required: No
- Default: `"Content"`

**Common values:**

- `"Description"` - General explanations
- `"Action"` - Intent-based lookups
- `"Effect"` - What happens when used
- `"Result"` - Output or outcome

---

### `metadata`

```json
{
  "metadata": {
    "source": "official-docs",
    "difficulty": "beginner"
  }
}
```

Freeform object for additional metadata not covered by the standard fields.

**Constraints:**

- Type: `object`
- Additional properties: Allowed
- Required: No

---

## Section Object

Sections group related entries together, forming cognitive "chunks."

```json
{
  "sections": [
    {
      "title": "Basic Commands",
      "description": "Commands you'll use every day",
      "entries": [...]
    }
  ]
}
```

### Fields

| Field         | Type     | Required | Description                                                                |
| ------------- | -------- | -------- | -------------------------------------------------------------------------- |
| `title`       | `string` | Yes      | The name of this section. Must be non-empty.                               |
| `description` | `string` | No       | Optional explanation of what this section covers.                          |
| `entries`     | `array`  | Yes      | The anchor-content pairs in this section. Must contain at least one entry. |

---

## Entry Object

Entries are the atomic units of a cheatsheet—individual anchor-content pairs.

```json
{
  "entries": [
    {
      "anchor": "git status",
      "content": "Show working tree status",
      "details": "Displays staged, unstaged, and untracked files. Use -s for short format.",
      "url": "https://git-scm.com/docs/git-status",
      "comments": "Most commonly used command"
    }
  ]
}
```

### Fields

| Field      | Type     | Required | Description                                                  |
| ---------- | -------- | -------- | ------------------------------------------------------------ |
| `anchor`   | `string` | Yes      | The retrieval anchor—what users scan for. Must be non-empty. |
| `content`  | `string` | Yes      | The associated content—what users need. Must be non-empty.   |
| `details`  | `string` | No       | Extended explanation, typically interpreted as markdown.     |
| `url`      | `string` | No       | Reference link to learn more (URI format).                   |
| `comments` | `any`    | No       | Additional notes, warnings, or context.                      |

### `anchor`

The retrieval anchor is what users visually scan for when looking up information. It should be distinctive and easy to spot.

**Examples by type:**

- CLI command: `"git commit -m \"<message>\""`
- Keyboard shortcut: `"Ctrl+K Ctrl+C"`
- Code pattern: `"array.map(fn)"`
- Config key: `"maxRetries"`

### `content`

The associated content is what users need once they find the anchor.

**Examples by retrieval direction:**

- Mechanism → Meaning: `"Create a commit with the specified message"`
- Intent → Mechanism: `"Save the current file"`

### `details`

Extended explanation beyond the brief content. Renderers typically interpret this as markdown, allowing for formatted text, lists, and code blocks.

```json
{
  "anchor": "git rebase -i",
  "content": "Interactive rebase for editing commit history.",
  "details": "Opens an editor with a list of commits. Commands:\n- `pick` - use commit\n- `squash` - meld into previous\n- `reword` - change message\n- `drop` - remove commit"
}
```

**Constraints:**

- Type: `string`
- Required: No

**When to use:**

- Complex commands needing step-by-step explanation
- Entries with multiple options or modes
- Runbook steps requiring detailed instructions

### `url`

Reference link to external documentation or resources.

```json
{
  "anchor": "git bisect",
  "content": "Binary search to find the commit that introduced a bug.",
  "url": "https://git-scm.com/docs/git-bisect"
}
```

**Constraints:**

- Type: `string`
- Format: URI
- Required: No

**When to use:**

- Link to official documentation
- Reference tutorials or guides
- Point to related tools or resources

### `comments`

Additional context that doesn't fit in the content field. Can be any JSON type, though strings are most common.

```json
{
  "anchor": "git push --force",
  "content": "Force push to remote (overwrites history)",
  "comments": "WARNING: Never use on shared branches"
}
```

---

## Complete Example

```json
{
  "$schema": "https://chson.dev/schema/v2/chson.schema.json",
  "title": "Git Core Commands",
  "version": "2.47.0",
  "publicationDate": "2026-02-15",
  "description": "Essential Git commands for daily workflows",
  "author": "ChSON Contributors",
  "license": "CC-BY-4.0",
  "homepage": "https://git-scm.com",
  "tags": ["cli", "version-control"],
  "retrievalDirection": "mechanism-to-meaning",
  "anchorLabel": "Command",
  "contentLabel": "Description",
  "sections": [
    {
      "title": "Getting Started",
      "description": "Initialize and clone repositories",
      "entries": [
        {
          "anchor": "git init",
          "content": "Create a new Git repository in the current directory"
        },
        {
          "anchor": "git clone <url>",
          "content": "Clone a repository from a remote URL"
        }
      ]
    },
    {
      "title": "Daily Workflow",
      "entries": [
        {
          "anchor": "git status",
          "content": "Show working tree status",
          "comments": "Use -s for short format"
        },
        {
          "anchor": "git add <file>",
          "content": "Stage changes for the next commit"
        },
        {
          "anchor": "git commit -m \"<msg>\"",
          "content": "Create a commit with the specified message"
        }
      ]
    }
  ]
}
```

---

## JSON Schema

The full JSON Schema (Draft 2020-12) is available at:

- **URL:** [https://chson.dev/schema/v2/chson.schema.json](https://chson.dev/schema/v2/chson.schema.json)
- **Source:** [`packages/chson-schema/schema/v2/chson.schema.json`](https://github.com/chson-dev/chson/blob/main/packages/chson-schema/schema/v2/chson.schema.json)

### TypeScript Types

Auto-generated TypeScript types are available in the `@chson/schema` package:

```typescript
import type { ChSONv2 } from '@chson/schema';

const cheatsheet: ChSONv2 = {
  title: "My Cheatsheet",
  publicationDate: "2026-02-15",
  description: "A sample cheatsheet",
  sections: [...]
};
```
