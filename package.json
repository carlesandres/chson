{
  "name": "chson",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "packageManager": "npm@11.10.1",
  "workspaces": [
    "packages/*",
    "apps/web"
  ],
  "scripts": {
    "prepare": "husky",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint:check": "turbo run lint:check",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "validate": "turbo run validate",
    "render": "turbo run render",
    "render:build": "turbo run render:build"
  },
  "devDependencies": {
    "favicons": "7.2.0",
    "husky": "9.1.7",
    "portless": "0.15.1",
    "turbo": "^2.8.10",
    "typescript": "6.0.2"
  },
  "overrides": {
    "typescript": "6.0.2"
  }
}
